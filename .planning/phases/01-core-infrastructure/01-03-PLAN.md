---
phase: 01-core-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/socialsim4/core/contagion/scene.py
  - tests/unit/test_contagion_scene.py
  - tests/integration/test_contagion_grid.py
autonomous: true
requirements:
  - GRID-01
  - GRID-02
  - GRID-03
  - GRID-04
  - HIDE-01
  - HIDE-02
  - HIDE-03
user_setup: []
must_haves:
  truths:
    - "Agents positioned on grid using GameMap infrastructure"
    - "Agent can see adjacent agents via Moore neighborhood (8 cells)"
    - "Agent sees only neighbor IDs, not their contagion states"
    - "Frontend receives all agent states for monitoring"
    - "Grid cells are all passable (open grid)"
  artifacts:
    - path: "src/socialsim4/core/contagion/scene.py"
      provides: "Grid integration with hidden state semantics"
      exports: ["ContagionScene.get_moore_neighbors", "ContagionScene.get_adjacent_agents", "ContagionScene.get_agent_status_prompt"]
  key_links:
    - from: "get_adjacent_agents"
      to: "agent.properties['map_xy']"
      via: "coordinate lookup"
      pattern: "agent\.properties\.get\('map_xy'\)"
    - from: "get_agent_status_prompt"
      to: "agent.properties['contagion_state']"
      via: "own state display"
      pattern: "contagion_state"
    - from: "contagion_stats event"
      to: "frontend"
      via: "WebSocket"
      pattern: "agent_states"
---

<objective>
Add grid integration and hidden state semantics to ContagionScene.

Purpose: Enable agents to see adjacent neighbors (IDs only) while keeping contagion states hidden from other agents. Frontend receives full state information for monitoring.
Output: Moore neighborhood queries, agent status prompt with own state, and frontend state exposure.
</objective>

<execution_context>
@C:/Users/Justin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Key patterns from existing codebase -->

From src/socialsim4/core/scenes/village_scene.py:
```python
class GameMap:
    def in_bounds(self, x: int, y: int) -> bool:
        return 0 <= x < self.width and 0 <= y < self.height

    def neighbors(self, x: int, y: int) -> Iterable[Tuple[int, int]]:
        """4-directional (von Neumann) neighbors"""
        for dx, dy in ((1, 0), (-1, 0), (0, 1), (0, -1)):
            nx, ny = x + dx, y + dy
            if self.is_passable(nx, ny):
                yield nx, ny

class VillageScene(Scene):
    def initialize_agent(self, agent: Agent):
        agent.properties["map_xy"] = [x, y]
        agent.properties["map_position"] = loc.name if loc else f"{x},{y}"

    def get_agent_status_prompt(self, agent: Agent) -> str:
        xy = agent.properties.get("map_xy") or [None, None]
        return f"Current position: ({xy[0]},{xy[1]})"
```

From src/socialsim4/core/simulator.py:
```python
def emit_event_later(self, event_type: str, data: dict):
    self.event_queue.put({"type": event_type, "data": data})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Moore neighborhood and adjacent agents query</name>
  <files>
    - src/socialsim4/core/contagion/scene.py
    - tests/unit/test_contagion_scene.py
  </files>
  <behavior>
    - Test 1: get_moore_neighbors(5, 5) returns 8 coordinates for center cell
    - Test 2: get_moore_neighbors(0, 0) returns 3 coordinates (corner)
    - Test 3: get_moore_neighbors filters out-of-bounds coordinates
    - Test 4: get_adjacent_agents returns agent names in adjacent cells
    - Test 5: get_adjacent_agents returns empty list for agent with no map_xy
    - Test 6: get_adjacent_agents does NOT include the querying agent
  </behavior>
  <action>
Add Moore neighborhood (8-directional) support to ContagionScene.

In `src/socialsim4/core/contagion/scene.py`, add methods to ContagionScene:

1. `get_moore_neighbors(self, x: int, y: int) -> list[tuple[int, int]]`:
   - Returns list of (x, y) tuples for all 8 surrounding cells
   - Iterate over dx in [-1, 0, 1] and dy in [-1, 0, 1]
   - Skip (0, 0) (the center cell)
   - Only include cells where `self.game_map.in_bounds(nx, ny)`
   - Moore neighborhood includes diagonals (unlike von Neumann which is 4-directional)

2. `get_adjacent_agents(self, agent_name: str, simulator: Simulator) -> list[str]`:
   - Get agent from simulator.agents[agent_name]
   - Get agent's coordinates from agent.properties.get("map_xy")
   - Return empty list if no coordinates
   - Get Moore neighbors for those coordinates
   - For each other agent in simulator.agents:
     - Skip if same as querying agent
     - Check if their map_xy is in the neighbors list
     - Add name to result if adjacent
   - Return list of agent names (strings)

Do NOT include other agents' contagion states in the return value - only names.
This implements HIDE-01 (hidden states from other agents).
  </action>
  <verify>
    <automated>pytest tests/unit/test_contagion_scene.py -x -k "moore or adjacent"</automated>
  </verify>
  <done>
Moore neighborhood returns 8-directional adjacent coordinates.
get_adjacent_agents returns only agent names (IDs), not their states.
Corner/edge cells return fewer neighbors correctly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement hidden state semantics in agent status prompt</name>
  <files>
    - src/socialsim4/core/contagion/scene.py
    - tests/unit/test_contagion_scene.py
  </files>
  <behavior>
    - Test 1: get_agent_status_prompt includes agent's own contagion state
    - Test 2: get_agent_status_prompt includes list of adjacent agent names
    - Test 3: get_agent_status_prompt does NOT include other agents' states
    - Test 4: Status prompt format matches existing VillageScene pattern with contagion additions
  </behavior>
  <action>
Override get_agent_status_prompt to include own state and adjacent agents (IDs only).

In `src/socialsim4/core/contagion/scene.py`, add to ContagionScene:

`get_agent_status_prompt(self, agent: Agent) -> str`:
- Override parent method to add contagion-specific status
- Get agent's coordinates from agent.properties.get("map_xy")
- Get location name if at a named location
- Get agent's contagion state from agent.properties.get("contagion_state", "unknown")
- Get contagion turns from agent.properties.get("contagion_turns", 0)
- Build status string including:
  - Position (from VillageScene pattern)
  - Own contagion state (e.g., "You are currently INFECTED")
  - Turns since infection (if applicable)
  - List of adjacent agent names (from get_adjacent_agents)

Example format:
```
--- Status ---
Current position: village_center at (10,10)
Contagion state: INFECTED (turns infected: 3)
Nearby agents: Alice, Bob, Charlie
```

Do NOT include other agents' contagion states in the prompt.
This implements HIDE-02 (agents infer states from behavior, not direct observation).
  </action>
  <verify>
    <automated>pytest tests/unit/test_contagion_scene.py -x -k "status_prompt"</automated>
  </verify>
  <done>
Agent status prompt shows own contagion state.
Agent status prompt shows adjacent agent names only (not their states).
Hidden state semantics enforced in agent context.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose all agent states to frontend for monitoring</name>
  <files>
    - src/socialsim4/core/contagion/scene.py
    - tests/unit/test_contagion_scene.py
  </files>
  <behavior>
    - Test 1: contagion_stats event includes agent_states dict
    - Test 2: agent_states maps agent_name -> state for all agents
    - Test 3: Frontend can render grid with state-based coloring using this data
  </behavior>
  <action>
Ensure frontend receives all agent states via contagion_stats event.

In `src/socialsim4/core/contagion/scene.py`, update `_update_statistics`:

The existing implementation from Plan 02 should already emit:
```python
simulator.emit_event_later("contagion_stats", {
    "counts": self._statistics.counts,
    "agent_states": {
        name: agent.properties.get("contagion_state")
        for name, agent in simulator.agents.items()
    },
})
```

Verify this is correctly implemented and add test coverage for:
- Event payload contains "agent_states" key
- agent_states includes all agents
- agent_states values are state strings (not enum objects)

This implements HIDE-03 (frontend sees true states for monitoring).
The frontend can use this data to render grid cells with state-based coloring.
  </action>
  <verify>
    <automated>pytest tests/unit/test_contagion_scene.py -x -k "frontend or agent_states"</automated>
  </verify>
  <done>
contagion_stats event includes agent_states with all agent states.
Frontend has data needed for state-based visualization.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Verify open grid configuration</name>
  <files>
    - tests/unit/test_contagion_scene.py
  </files>
  <behavior>
    - Test 1: Default GameMap has all passable cells
    - Test 2: Agents can be positioned at any valid coordinate
    - Test 3: No obstacles block movement for v1.0
  </behavior>
  <action>
Add tests verifying open grid behavior for contagion scenes.

In `tests/unit/test_contagion_scene.py`:

1. Test that default GameMap (20x20) has all passable tiles:
   - Create GameMap(20, 20)
   - Verify game_map.is_passable(x, y) for sample of coordinates
   - Verify no blocking terrain

2. Test that ContagionScene with default map allows any position:
   - Create ContagionScene with default GameMap
   - Initialize agents at various positions
   - Verify all positions valid

This implements GRID-04 (open grid, no obstacles for v1.0).

Note: Obstacles and terrain are deferred to v2 per user decision.
  </action>
  <verify>
    <automated>pytest tests/unit/test_contagion_scene.py -x -k "open_grid"</automated>
  </verify>
  <done>
Tests confirm open grid behavior for contagion scenes.
All cells passable by default.
  </done>
</task>

<task type="auto">
  <name>Task 5: Integration test for grid positioning and hidden states</name>
  <files>
    - tests/integration/test_contagion_grid.py
  </files>
  <action>
Create integration test for end-to-end grid positioning with hidden states.

Create `tests/integration/test_contagion_grid.py`:

1. Test full scenario with multiple agents on grid:
   - Create ContagionScene with GameMap
   - Create 5 agents with random positions
   - Run pre_run to initialize states
   - Verify all agents have valid map_xy
   - Verify all agents have contagion_state set
   - Call get_adjacent_agents for each agent
   - Verify only adjacent agents are returned
   - Verify states are NOT included in adjacent agent info

2. Test status prompt integration:
   - Get status prompt for infected agent
   - Verify prompt contains own state
   - Verify prompt contains adjacent agent names
   - Verify prompt does NOT contain other agents' states

3. Test frontend data:
   - Run _update_statistics
   - Capture emit_event_later call
   - Verify agent_states contains all agents
   - Verify counts match actual agent distribution
  </action>
  <verify>
    <automated>pytest tests/integration/test_contagion_grid.py -x</automated>
  </verify>
  <done>
Integration tests pass for grid positioning with hidden states.
End-to-end flow verified.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `pytest tests/unit/test_contagion_scene.py tests/integration/test_contagion_grid.py -v`
2. Verify Moore neighborhood: `python -c "from socialsim4.core.contagion import ContagionScene; ..."`
3. Manual verification: Create small simulation, check agent prompts show only IDs for neighbors
</verification>

<success_criteria>
- [ ] Moore neighborhood (8-directional) implemented
- [ ] get_adjacent_agents returns only agent names (hidden states)
- [ ] Agent status prompt shows own state but not others' states
- [ ] Frontend receives all agent states via contagion_stats event
- [ ] Open grid configuration verified (all cells passable)
- [ ] Integration tests pass for grid positioning with hidden states
- [ ] All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-03-SUMMARY.md`
</output>
