---
phase: 01-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/socialsim4/core/contagion/__init__.py
  - src/socialsim4/core/contagion/states.py
  - src/socialsim4/core/contagion/rules.py
autonomous: true
requirements:
  - CORE-01
  - CORE-02
user_setup: []
must_haves:
  truths:
    - "ContagionState enum defines SUSCEPTIBLE, INFECTED, RECOVERED, EXPOSED values"
    - "StateTransition dataclass accepts from_state, to_state, trigger_type, probability, decay_turns"
    - "StateTransition validates probability is 0.0-1.0 and trigger_type is valid"
  artifacts:
    - path: "src/socialsim4/core/contagion/states.py"
      provides: "ContagionState enum definition"
      exports: ["ContagionState"]
    - path: "src/socialsim4/core/contagion/rules.py"
      provides: "StateTransition dataclass for rule definitions"
      exports: ["StateTransition", "check_probability"]
  key_links:
    - from: "rules.py"
      to: "states.py"
      via: "import ContagionState"
      pattern: "from .states import ContagionState"
---

<objective>
Create the contagion state enum and transition rule dataclass.

Purpose: Define the foundational types for the contagion framework that all other components will depend on.
Output: New module `src/socialsim4/core/contagion/` with states.py and rules.py.
</objective>

<execution_context>
@C:/Users/Justin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-infrastructure/01-CONTEXT.md
@.planning/phases/01-core-infrastructure/01-RESEARCH.md

<interfaces>
<!-- Key patterns from existing codebase for executor reference -->

From src/socialsim4/core/scenes/council_scene.py (phase_controller.py pattern):
```python
from enum import Enum

class CouncilPhase(str, Enum):
    """Phases of council deliberation."""
    DISCUSSION = "discussion"
    VOTING = "voting"
```

From src/socialsim4/core/environment_config.py (dataclass pattern):
```python
from dataclasses import dataclass, field
from typing import Optional

@dataclass
class EnvironmentConfig:
    enabled: bool = False
    turn_interval: int = 5
```

From src/socialsim4/core/scene.py (Scene.state pattern):
```python
self.state = {"time": 1080}  # Runtime mutable state
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ContagionState enum</name>
  <files>
    - src/socialsim4/core/contagion/__init__.py
    - src/socialsim4/core/contagion/states.py
    - tests/unit/test_contagion_states.py
  </files>
  <behavior>
    - Test 1: ContagionState.SUSCEPTIBLE equals "susceptible"
    - Test 2: ContagionState.INFECTED equals "infected"
    - Test 3: ContagionState.RECOVERED equals "recovered"
    - Test 4: ContagionState.EXPOSED equals "exposed"
    - Test 5: All states are string enums (inherit from str, Enum)
  </behavior>
  <action>
Create the contagion module structure and ContagionState enum.

1. Create `src/socialsim4/core/contagion/__init__.py` with module docstring and exports
2. Create `src/socialsim4/core/contagion/states.py`:
   - File header docstring explaining the module purpose
   - Import Enum from stdlib
   - Define `ContagionState(str, Enum)` with values:
     - SUSCEPTIBLE = "susceptible"
     - EXPOSED = "exposed"
     - INFECTED = "infected"
     - RECOVERED = "recovered"
   - Follow the CouncilPhase pattern from council_scene.py (str, Enum for JSON serialization)
3. Create `tests/unit/test_contagion_states.py` with unit tests per behavior spec above

Do NOT add any extra states beyond the four specified (future states can be added later).
Do NOT add metadata fields to the enum - keep it simple per user decision.
  </action>
  <verify>
    <automated>pytest tests/unit/test_contagion_states.py -x</automated>
  </verify>
  <done>
ContagionState enum exists with SUSCEPTIBLE, INFECTED, RECOVERED, EXPOSED values.
All values are lowercase strings matching the enum names.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create StateTransition dataclass</name>
  <files>
    - src/socialsim4/core/contagion/rules.py
    - tests/unit/test_contagion_rules.py
  </files>
  <behavior>
    - Test 1: StateTransition accepts from_state, to_state, trigger_type, probability
    - Test 2: StateTransition validates probability must be 0.0-1.0 (raises ValueError otherwise)
    - Test 3: StateTransition validates trigger_type must be "proximity", "action", or "decay"
    - Test 4: StateTransition requires decay_turns when trigger_type is "decay" (raises ValueError if missing)
    - Test 5: decay_turns is Optional[int] and defaults to None
    - Test 6: check_probability(prob) returns True when random.random() < prob
  </behavior>
  <action>
Create the StateTransition dataclass for defining contagion rules.

1. Create `src/socialsim4/core/contagion/rules.py`:
   - File header docstring explaining rule evaluation purpose
   - Import dataclass, Optional from dataclasses
   - Import random from stdlib
   - Import ContagionState from .states
   - Define `@dataclass class StateTransition`:
     - from_state: ContagionState
     - to_state: ContagionState
     - trigger_type: str (validated in __post_init__)
     - probability: float (validated 0.0-1.0 in __post_init__)
     - decay_turns: Optional[int] = None
   - Implement `__post_init__` with validation:
     - Raise ValueError if probability not in [0.0, 1.0]
     - Raise ValueError if trigger_type not in ("proximity", "action", "decay")
     - Raise ValueError if trigger_type == "decay" and decay_turns is None
   - Define `check_probability(probability: float) -> bool`:
     - Returns `random.random() < probability`
2. Create `tests/unit/test_contagion_rules.py` with unit tests per behavior spec above
3. Update `__init__.py` to export StateTransition and check_probability

Follow LLMConfig/EnvironmentConfig dataclass patterns from existing codebase.
Do NOT add additional validation beyond what's specified.
  </action>
  <verify>
    <automated>pytest tests/unit/test_contagion_rules.py -x</automated>
  </verify>
  <done>
StateTransition dataclass exists with validation.
check_probability function exists for probability checks.
Invalid probability or trigger_type raises ValueError with clear message.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `pytest tests/unit/test_contagion*.py -v` - all tests pass
2. Verify imports work: `python -c "from socialsim4.core.contagion import ContagionState, StateTransition"`
3. Verify enum values: `python -c "from socialsim4.core.contagion import ContagionState; print(ContagionState.INFECTED.value)"`
</verification>

<success_criteria>
- [ ] ContagionState enum defines 4 states as lowercase string values
- [ ] StateTransition dataclass validates probability range and trigger_type
- [ ] StateTransition requires decay_turns when trigger_type is "decay"
- [ ] check_probability function implements random probability check
- [ ] All unit tests pass
- [ ] Module follows project file header standards
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-infrastructure/01-01-SUMMARY.md`
</output>
